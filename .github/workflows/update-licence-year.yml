name: Update License Year

on:
  schedule:
    - cron: '0 0 1 1 *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-license-year:
    runs-on: ubuntu-latest

    steps:
      - name: Update License Year on main
        uses: EduardaSRBastos/update-license-year-action@main
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main

      - name: Update License Year on develop
        uses: EduardaSRBastos/update-license-year-action@main
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: develop
